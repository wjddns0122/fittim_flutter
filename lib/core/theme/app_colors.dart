import 'package:flutter/cupertino.dart';

class AppColors {
  // Primary & Backgrounds
  static const Color backgroundPrimary = Color(0xFFFFFFFF);
  static const Color backgroundSecondary = Color(0xFFFAFAFA);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF9C9C9C); // Updated from #5A5A5A
  static const Color textHint = Color(0xFF9C9C9C);

  // Accents & Buttons
  static const Color buttonPrimary = Color(0xFF1A1A1A);
  static const Color buttonText = Color(0xFFFFFFFF);

  // Borders & Dividers
  static const Color border = Color(0xFFEAEAEA);

  // Social
  static const Color kakaoYellow = Color(0xFFFEE500);
  static const Color kakaoText = Color(0xFF000000);

  // Weather / Brand Accents
  static const Color brandBlue = Color(0xFF74A8FF);

  // Destructive
  static const Color destructive = CupertinoColors.systemRed;
}
